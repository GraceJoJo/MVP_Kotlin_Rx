<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jojo.mvp_kotlin">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseTheme">
        <activity android:name=".TestStatusActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestLambda"></activity>
        <activity android:name=".ACT_Splsh"></activity>
        <activity android:name=".MainActivity"></activity>
        <!--<activity android:name=".mvvm.TestDataBinding"></activity>-->
        <!--<activity android:name=".mvvm.model.TestDBActivity"></activity>-->
        <activity android:name=".mvc.TestMvcActivity"></activity>
        <activity android:name=".mvp.ui.TestActivity"></activity>
        <!--<activity android:name=".TestLambda" />-->
    </application>

</manifest>